generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

model User {
  id           Int           @id @default(autoincrement())
  email        String        @unique
  password     String
  transactions Transaction[] // Relation: One user has many transactions
  invoices     Invoice[]
}

model Invoice {
  id       Int      @id @default(autoincrement())
  payee    String
  amount   Float
  date     DateTime @default(now())
  dueDate  DateTime
  status   String   @default("Pending") // Unpaid, Paid, Pending
  
  userId   Int
  user     User     @relation(fields: [userId], references: [id])
}

model Transaction {
  id       Int      @id @default(autoincrement())
  text     String?
  amount   Float
  category String   @default("Other")
  date     DateTime @default(now())
  
  // Link to User (Foreign Key)
  userId   Int
  user     User     @relation(fields: [userId], references: [id])
}